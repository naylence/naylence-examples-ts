{
  "name": "distributed-multi-agent-example",
  "version": "1.0.0",
  "type": "module",
  "description": "Multi-agent text analysis pipeline demonstrating distributed agent orchestration",
  "scripts": {
    "build": "tsc && npm run rename-to-mjs && npm run fix-imports",
    "rename-to-mjs": "find dist -name '*.js' -exec sh -c 'mv \"$1\" \"${1%.js}.mjs\"' _ {} \\;",
    "fix-imports": "find dist -name '*.mjs' -exec sed -i '' 's/\\.js/.mjs/g' {} \\;"
  },
  "dependencies": {
    "@naylence/agent-sdk": "^0.3.3",
    "openai": "^4.73.1"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}

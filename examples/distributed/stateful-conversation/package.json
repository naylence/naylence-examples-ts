{
  "name": "@naylence-examples/distributed-stateful-conversation",
  "version": "0.1.0",
  "description": "Stateful conversation chat agent example with LLM integration",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "tsc && npm run rename-to-mjs && npm run fix-imports",
    "rename-to-mjs": "find dist -name '*.js' -exec sh -c 'mv \"$1\" \"${1%.js}.mjs\"' _ {} \\;",
    "fix-imports": "find dist -name '*.mjs' -exec sed -i '' 's/\\.js/.mjs/g' {} \\;"
  },
  "dependencies": {
    "@naylence/agent-sdk": "^0.3.3",
    "openai": "^4.73.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.3"
  }
}
